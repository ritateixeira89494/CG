#ifndef __SPHERE_H__
#define __SPHERE_H__

#include <string.h>
/*!
    In this function we open and close the necessary file, and draw to the file the points that belong to the sphere.
*/
void drawSphere(int radius, int slices, int stacks, std::string filename);

#endif // __SPHERE_H__
